@model IEnumerable<PAW3.Models.DTOs.InventoryDTO>
@{
    ViewData["Title"] = "Inventario";
}

<div class="container">
    <div class="d-flex justify-content-between align-items-center mb-3 flex-wrap gap-2">
        <h1 class="display-6 m-0">Inventario</h1>

        <form asp-controller="Inventory" asp-action="Index" method="get" class="d-flex align-items-center gap-2">
            <input type="number" class="form-control" name="id" min="1" placeholder="Buscar por ID..." style="max-width: 220px;" />
            <button type="submit" class="btn btn-outline-light">Buscar</button>
            <a asp-controller="Inventory" asp-action="Index" class="btn btn-secondary">Limpiar</a>
        </form>

        <a class="btn btn-success" asp-controller="Inventory" asp-action="Create">
            Crear Nuevo
        </a>
    </div>

    @if (ViewBag.Info != null)
    {
        <div class="alert alert-info">@ViewBag.Info</div>
    }

    <table class="table table-dark table-striped">
        <thead>
            <tr>
                <th>Id</th>
                <th>UnitPrice</th>
                <th>UnitsInStock</th>
                <th>LastUpdated</th>
                <th>DateAdded</th>
                <th style="width: 180px;">Acciones</th>
            </tr>
        </thead>
        <tbody>
            @if (Model != null && Model.Any())
            {
                foreach (var item in Model)
                {
                    <tr>
                        <td>@item.InventoryId</td>
                        <td>@item.UnitPrice</td>
                        <td>@item.UnitsInStock</td>
                        <td>@item.LastUpdated</td>
                        <td>@item.DateAdded</td>
                        <td>
                            <a class="btn btn-sm btn-primary me-2"
                               asp-controller="Inventory" asp-action="Update" asp-route-id="@item.InventoryId">
                                Actualizar
                            </a>
                            <form class="d-inline" method="post"
                                  asp-controller="Inventory" asp-action="Delete" asp-route-id="@item.InventoryId"
                                  onsubmit="return confirm('¿Seguro que deseas borrar el inventario @item.InventoryId?');">
                                @Html.AntiForgeryToken()
                                <button type="submit" class="btn btn-sm btn-danger">Borrar</button>
                            </form>
                        </td>
                    </tr>
                }
            }
            else
            {
                <tr><td colspan="7" class="text-center">No hay registros de inventario.</td></tr>
            }
        </tbody>
    </table>
</div>